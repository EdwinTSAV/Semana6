@{
    //var cuenta = (Cuenta)ViewBag.Account; // FORMA a
    //var cuenta = (Cuenta)Model; // FORMA B
    Layout = null;
}

@*<body>
    <header>
        <div class="container">
            <h1 class="h-100 badge badge-dark">Usuario: @User.Claims.FirstOrDefault().Value</h1>
        </div>
    </header>
</body>*@

<!--><h1>Editar Cuenta</h1></!-->
<div class="row">
    <div class="col-6">
        <form id="editarPrueba" onsubmit="editarCuenta(event)" action="/crud/editar" enctype="multipart/form-data" method="post">

            <input id="mtxtId" type="hidden" name="Id" @*value="@cuenta.Id"*@ />

            <div class="form-group">
                <label>Imagen</label>
            </div>
            <div class="form-group">
                <input id="mtxtImage" type="file" name="Image" accept="image/x-png,image/gif,image/jpeg" />
            </div>

            <div class="form-group">
                <label>Tipo</label>
                <select id="mtxtTipo" name="TypeId" class="form-control">
                    <option value="">-- Selecciona una opcion --</option>
                    @foreach (var item in ViewBag.Types)
                    {
                        <option value="@item.Id">@item.Name</option>
                    }
                </select>
            </div>

            <div class="form-group">
                <label>Nombre</label>
                <input id="mtxtNombre" class="form-control" name="Name" @*value="@cuenta.Name"*@ />
            </div>

            <div class="form-group">
                <label>Divisa</label>
                <select id="mtxtDivisa" name="Currency" class="form-control">
                    <option value="">-- Selecciona una opcion --</option>
                    @foreach (var item in ViewBag.Currency)
                    {
                        <option value="@item">@item</option>
                    }
                </select>
            </div>

            <div class="form-group">
                <label>Saldo Inicial</label>
                <input id="mtxtAmount" class="form-control" name="Amount" @*value="@cuenta.Amount"*@ />
            </div>
        </form>
    </div>
</div>