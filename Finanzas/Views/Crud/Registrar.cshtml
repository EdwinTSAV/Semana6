@{ Layout = null;
    var cuenta = (Cuenta)Model; }

@*<body>
        <header>
            <div class="container">
                <h1 class="h-100 badge badge-dark">Usuario: @User.Claims.FirstOrDefault().Value</h1>
            </div>
        </header>
    </body>

    <h1>Crear Cuenta</h1>*@

<div class="row">
    <div class="col-6">
        @*Aca mando el Id para buscar directamente aca en el formulario: 
          El onsubmit() es propio del formulario, añado eso para que me aparezca al modal por si hubo un error, y no me bote otra url
          Mando el event en el onsubmit() porque lo quiero detener :'D*@
        <form action="/crud/registrar" id="createPrueba" onsubmit="guardarCuenta(event)" enctype="multipart/form-data" method="post">
            <div class="form-group">
                <label>Imagen</label>
            </div>
            <div class="form-group">
                <input id="imagen" type="file" name="Image" accept="image/x-png,image/gif,image/jpeg" />
            </div>


            <div class="form-group">
                <label>Tipo</label>
                <select name="TypeId" class="form-control">
                    <option value="">-- Selecciona una opcion --</option>
                    @foreach (var item in ViewBag.Types)
                    {
                        <option value="@item.Id">@item.Name</option>
                    }
                </select>
            </div>

            <div class="form-group">
                <label>Nombre</label>
                <input class="form-control" name="Name" value="@Model.Name" />
                @Html.ValidationMessage("Name")
            </div>

            <div class="form-group">
                <label>Divisa</label>
                <select name="Currency" class="form-control">
                    <option value="">-- Selecciona una opcion --</option>
                    @foreach (var item in ViewBag.Currency)
                    {
                        <option value="@item">@item</option>
                    }
                </select>
            </div>

            <div class="form-group">
                <label>Saldo Inicial</label>
                <input class="form-control" name="Amount" value="@Model.Amount"/>
                @Html.ValidationMessage("Amount")
            </div>
        </form>
    </div>
</div>